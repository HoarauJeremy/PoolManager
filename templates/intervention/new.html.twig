{% extends 'base.html.twig' %}

{% block title %}Nouvelle Intervention
{% endblock %}

{% block body %}
	<div class="min-h-screen bg-blue-50 py-8 px-4">
		<div class="max-w-3xl mx-auto">
			<div class="bg-blue-200 rounded-2xl shadow-lg p-8">
				<h1 class="text-center text-3xl font-bold text-gray-800 mb-8">
					Créer une nouvelle Intervention
				</h1>

				{{ include('intervention/_form.html.twig') }}

				<div class="mt-6">
					<a href="{{ path('app_intervention_index') }}" class="inline-block text-sm font-bold text-blue-600 hover:text-blue-800 transition-colors">
						← Retour à la liste
					</a>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
